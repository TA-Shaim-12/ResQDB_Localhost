# Copy this file to .env and fill in your actual MySQL credentials.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=resqdb

PORT=4000
CORS_ORIGIN=http://localhost:5173

# Used to sign login tokens — any random string works, change this for real use
AUTH_SECRET=replace_with_any_random_string
